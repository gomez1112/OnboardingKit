# Getting Started

Create steps with ``OnboardingBuilder`` and present them in ``OnboardingFlow``. Custom required content reports validation reactively with ``SwiftUI/View/onboardingStepComplete(_:)``. Use ``OnboardingWrapper`` to persist the app version and automatically select first-launch or What's New content.
